systemLog:
  destination: file
  logAppend: true
  path: {{ mongos_log }}
processManagement:
  fork: true
  pidFilePath: /var/run/mongod.pid
sharding:
  configDB: {{ mongos_cfg }}
security:
  keyFile: "/etc/{{ mongos_keyfile }}"
net:
  port: 27017
  bindIp: 0.0.0.0
